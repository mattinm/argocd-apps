{{- range .Values.applications }}
{{- $config := $.Values.config -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .name | quote }}
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: {{ .namespace | default .name | quote }}
    server: {{ $config.spec.destination.server | quote }}
  project: default
  source:
    - repoURL: {{ .helm.repoURL | quote }}
      chart: {{ .helm.chart }}
      targetRevision: {{ .helm.targetRevision }}
      helm:
        valueFiles:
          {{- range (.helm).valueFiles | default $config.spec.source.helm.valueFiles }}
          - {{ . }}
          {{- end }}
    - path: {{ .path | default .name | quote }}
      repoURL: {{ $config.spec.source.repoURL | quote }}
      targetRevision: {{ $config.spec.source.targetRevision }}
      ref: values
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
---
{{ end -}}
