config:
  spec:
    destination:
      server: https://kubernetes.default.svc
    source:
      repoURL: https://github.com/mattinm/argcd-apps.git
      targetRevision: master

applications:
  - name: cert-manager
