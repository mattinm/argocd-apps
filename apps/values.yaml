config:
  spec:
    destination:
      server: https://kubernetes.default.svc
    source:
      repoURL: https://github.com/mattinm/argocd-apps.git
      targetRevision: master

applications:
  - name: cert-manager
